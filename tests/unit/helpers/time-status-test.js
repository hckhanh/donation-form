import { timeStatus } from 'donation-form/helpers/time-status';
import { module, test } from 'qunit';

module('Unit | Helper | time status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeStatus([42]);
  assert.ok(result);
});
